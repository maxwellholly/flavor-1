import React from 'react'
import Form from '../form/Form'

const Day = props => {
    return (
        null
    )
};

export default Day

const styles = {
    form: {
        marginTop: '300px'
    }
};